server:
  port: 0

spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

  cloud:
    config:
      name: application
  config:
    import: configserver:http://localhost:8888
  application:
    name: user-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password: 12345

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
        defaultZone: http://localhost:8761/eureka/

greeting:
  message: Welcome to the simple E-commerce application

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, busrefresh

logging:
  level:
    com.example.userservice.client: DEBUG




#token:
#  expiration_time: 86400000
#  secret: fdsafdsafdsafdsafdsafdsafdsafdsafdsfdsafdsafdsafdsafdsafdsafdsafdsafdsfdsafdsafdsafdsafdsafdsafdsafdsafdsfdsafdsafdsafdsafdsafdsafdsafdsafds
